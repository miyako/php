# _PHPUI_Controller : _PHP_Controller : _CLI_Controller

`_PHPUI_Controller` is an example subclass of `_CLI_Controller` for the `PHP` class with a UI. 

The response is passed on to `Form.push()` for processing.